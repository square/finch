
stage:
  name: setup
  trx:
    - file: trx/create-tables.sql
    - file: trx/insert-rows.sql
      data:
        k:
          generator: "rand-int"
          params:
            max: 65536
        c:
          generator: "str-not-null"
          params:
            len: 119 # not a typo
        pad:
          generator: "str-not-null"
          params:
            len: 59 # not a typo
    - file: trx/analyze-tables.sql
